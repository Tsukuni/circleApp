<%= form_with(model: trip_rank, local: true) do |form| %>
  <% if trip_rank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip_rank.errors.count, "error") %> prohibited this trip_rank from being saved:</h2>

      <ul>
      <% trip_rank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :qestion1 %>
    <%= form.text_field :qestion1, id: :trip_rank_qestion1 %>
  </div>

  <div class="field">
    <%= form.label :qt1_rank1 %>
    <%= form.text_field :qt1_rank1, id: :trip_rank_qt1_rank1 %>
  </div>

  <div class="field">
    <%= form.label :qt1_rank2 %>
    <%= form.text_field :qt1_rank2, id: :trip_rank_qt1_rank2 %>
  </div>

  <div class="field">
    <%= form.label :qt1_rank3 %>
    <%= form.text_field :qt1_rank3, id: :trip_rank_qt1_rank3 %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :trip_rank_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

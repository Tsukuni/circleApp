<%= form_with(model: capacity, local: true) do |form| %>
  <% if capacity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(capacity.errors.count, "error") %> prohibited this capacity from being saved:</h2>

      <ul>
      <% capacity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :capacity_image %>
  </div>

  <div class="field">
    <%= form.label :context %>
    <%= form.text_area :context, id: :capacity_context %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
